[tool.poetry]
name = "couchlink"
version = "0.1.0"
description = "Couchlink DualSense helpers"
packages = [{ include = "couchlink" }]

[tool.poetry.dependencies]
python = "^3.10"
hidapi = "^0.14"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
