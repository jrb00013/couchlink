# WireGuard examples for couchlink

Templates: `wg0-host.conf.example`, `wg0-player.conf.example`.

Generate real configs (gitignored):

```bash
./scripts/setup-wireguard.sh
```

PRIME mesh overview (Tailscale + WireGuard): [`docs/MESH.md`](../../docs/MESH.md)  
WireGuard runbook: [`docs/WIREGUARD.md`](../../docs/WIREGUARD.md)
